I want to create a project for automating test case generation (unit, integration, and e2e). I want to pass project path only. The approach is, first I will create an agent which will create a json tree structure of project (by ignoring files mentioned in .gitignore) and then I will pass the json tree structure to llm model to detect the project type, ie, python or javascript or another, and the package file, ie, package.json or requirements.txt, or pyproject,toml, and then I will pass the file to another agent teslib-detector to parse the config package file and find out most suitable list of libraries for unit, integration, and e2e testing. then I will pass this libraries to another agent called installer agent, which will install the libraries in provided project path. After the libraries are installed, an agent will find out to create test config files, or update the existing test config files, and create a simple base test case. After this is done, another agent will run the test files, to check if base setup is done correctly or not. If not done correctly, it will send the error log to analyzer and then again update the config files based on error. This loop will coninue till the base test case not exeutes successfully. After that, another agent will utilize the project tree structure, and will create a \*.test.<ex based on project type, ie, tsx, ts or py> and place all the test cases for the given file for unit test case generation. This will be looped over till all test cases are generated. Once this is done. Another agent integration_test_agent will utilzie the project_path and features, to geenrate integration test cases, and similarly after this is done, another agent will do end to end test case genration. After all test case generated, a testing_agent will run these test cases, and on getting unit test error, it will collect the log and run the unit test creator agent agin to fix the issue, i all unit test cases are fixed, then will test the integration test cases. Tjis will continue till all the cases are generated and tested.

Utilize the above project structure to create a whole working project with langraph and the model is running as llama3.1 8b use langchain_openai's ChatOpenAI. Create individual files and folder for the sates, memories, routes, agents, and models where required
